# 异常检测逻辑说明

本文档描述当前仓库中已经落地的异常检测实现，重点对应 [latency_detector.py](C:/Users/10157/Desktop/xds/code/latency_detector.py) 和 [webapp.py](C:/Users/10157/Desktop/xds/code/webapp.py) 的实际行为。

当前版本的核心目标是：

- 用 `TTFT` 和 `TPOT` 作为系统异常事件的主判定指标
- 用 `RPM / TPM / 输入长度 / 输出长度` 作为异常阶段的根因分析依据
- 额外识别“新用户加入事件”
- 将检测结果、诱因判断和耗时拆解同步展示到网页中

## 1. 输入数据格式

检测器当前消费的是已经聚合好的 Excel sheet 数据。每个 sheet 表示一个池子，行级粒度是：

- 单个用户 `domain_id`
- 单个时间窗 `collect_time_std`

必需列如下：

| 列名 | 含义 |
| --- | --- |
| `domain_id` | 用户标识 |
| `rpm` | 当前时间窗内请求量 |
| `tpm` | 当前时间窗内 token 吞吐量 |
| `ttft_avg` | 当前时间窗内 TTFT 平均值，单位为 `ms` |
| `tpot_avg` | 当前时间窗内 TPOT 平均值，单位为 `ms` |
| `prompt_tokens` | 当前时间窗内平均输入长度 |
| `completion_tokens` | 当前时间窗内平均输出长度 |
| `collect_time_std` | 标准化时间字段 |

对应代码中的必需列定义见 [latency_detector.py:26](C:/Users/10157/Desktop/xds/code/latency_detector.py#L26)。

## 2. 输入预处理

### 2.1 基础校验

检测开始后，首先会调用 `validate_latency_input()` 进行输入校验：

- 检查必需列是否齐全
- `domain_id` 统一转成字符串并去掉空值
- 数值列统一转成数值，非法值转为 `0`
- `collect_time_std` 优先按 `%Y-%m-%d %H:%M:%S` 解析
- 如果固定格式失败，再走一次宽松时间解析
- 无法解析时间的行会被丢弃

对应代码见 [latency_detector.py:47](C:/Users/10157/Desktop/xds/code/latency_detector.py#L47)。

### 2.2 重复数据折叠

如果同一个 `domain_id + collect_time_std` 有多行数据，会先折叠成一行，逻辑如下：

- `rpm`：求和
- `tpm`：求和
- `ttft_avg`：按 `rpm` 做加权平均，并过滤掉值为 `0` 的点
- `tpot_avg`：按 `rpm` 做加权平均，并过滤掉值为 `0` 的点
- `prompt_tokens`：按 `rpm` 做加权平均
- `completion_tokens`：按 `rpm` 做加权平均

这里对 `TTFT/TPOT=0` 的处理很重要。当前实现认为：

- 这些 `0` 更可能表示当时没有有效统计到时延
- 因此在做时延均值时不参与计算

对应代码见 [latency_detector.py:89](C:/Users/10157/Desktop/xds/code/latency_detector.py#L89)。

## 3. 时间轴构建

系统会从所有有效时间点中推断时间粒度：

- 如果只有一个时间点，默认按 `1h`
- 否则取相邻时间点之间的最小正间隔作为频率

然后用这个频率补齐整个时间轴，生成连续的 `time_index`。  
也就是说，即使某些时间窗原始数据缺失，矩阵化后这些时间窗也会补出来，缺失位置以 `0` 填充。

对应代码见：

- [latency_detector.py:109](C:/Users/10157/Desktop/xds/code/latency_detector.py#L109)
- [latency_detector.py:383](C:/Users/10157/Desktop/xds/code/latency_detector.py#L383)

## 4. 用户矩阵与系统级序列

在统一时间轴上，系统会构建以下用户矩阵：

- `rpm_matrix`
- `tpm_matrix`
- `ttft_matrix`
- `tpot_matrix`
- `prompt_matrix`
- `completion_matrix`

矩阵维度是：

- 行：用户 `domain_id`
- 列：时间窗

在矩阵基础上，再构建系统级序列：

- `system_rpm = 所有用户 rpm 求和`
- `system_tpm = 所有用户 tpm 求和`
- `system_ttft = 所有用户 ttft_avg 按 rpm 加权平均，且忽略 0`
- `system_tpot = 所有用户 tpot_avg 按 rpm 加权平均，且忽略 0`
- `system_prompt = 所有用户 prompt_tokens 按 rpm 加权平均`
- `system_completion = 所有用户 completion_tokens 按 rpm 加权平均`

对应代码见 [latency_detector.py:390](C:/Users/10157/Desktop/xds/code/latency_detector.py#L390)。

## 5. 异常事件判定

### 5.1 当前判定目标

系统级异常事件不再基于流量突增，而是改为基于时延质量指标：

- `TTFT` SLA = `25000 ms`
- `TPOT` SLA = `45 ms`

配置定义见 [latency_detector.py:12](C:/Users/10157/Desktop/xds/code/latency_detector.py#L12)。

### 5.2 重度异常

单个时间窗内，如果某个指标显著超过 SLA，则直接判为异常：

- `TTFT 重度异常`：`system_ttft >= ttft_sla * severe_ratio`
- `TPOT 重度异常`：`system_tpot >= tpot_sla * severe_ratio`

默认 `severe_ratio = 1.3`。网页侧还有 3 档灵敏度映射：

- 灵敏：`1.1`
- 均衡：`1.3`
- 宽松：`1.5`

### 5.3 轻度持续异常

如果单个时间窗只是轻微超过 SLA，则不会立刻判成事件，而是要求持续多个时间窗：

- `TTFT 轻度异常`：`system_ttft > ttft_sla`
- `TPOT 轻度异常`：`system_tpot > tpot_sla`
- 默认需要连续 `2` 个时间窗

因此，轻度异常的核心思想是：

- 单点轻微超标，不一定算事件
- 连续多个时间窗轻微超标，说明问题具有持续性，也算事件

### 5.4 TTFT 和 TPOT 的合并规则

当前规则是：

- `TTFT` 或 `TPOT` 任一异常，则该时刻视为异常时刻
- 两者取并集，不要求同时命中

代码实现是：

- `sys_anom_ttft = ttft_heavy | mark_runs(ttft_mild, N)`
- `sys_anom_tpot = tpot_heavy | mark_runs(tpot_mild, N)`
- `sys_anom = sys_anom_ttft | sys_anom_tpot`

对应代码见 [latency_detector.py:421](C:/Users/10157/Desktop/xds/code/latency_detector.py#L421)。

### 5.5 事件窗口生成

异常时刻掩码生成之后，会继续做两步：

1. 把连续异常时刻合并成事件窗口
2. 如果事件太多，则按严重程度保留前 `max_events` 个事件

默认配置：

- `event_merge_gap = 0`
- `max_events = 5`

这意味着：

- 默认只合并真正相邻的异常窗
- 页面默认最多展示 5 个最重要事件

## 6. 基线计算

根因分析不直接对原始值做判断，而是先给每个用户建立历史基线。

当前基线使用滚动平均，核心参数如下：

- `baseline_window_points = 24`
- `min_baseline_points = 6`

也就是：

- 优先使用前 24 个时间窗的数据估计该用户的历史基线
- 至少需要 6 个点才开始形成可靠基线
- 基线只看过去，不看当前和未来

系统级也会分别为下面 4 类指标建立基线：

- `system_rpm`
- `system_tpm`
- `system_prompt`
- `system_completion`

对应代码见 [latency_detector.py:439](C:/Users/10157/Desktop/xds/code/latency_detector.py#L439)。

## 7. 根因定位逻辑

### 7.1 根因分析的总体思路

这里要把“事件判定”和“根因定位”区分开：

- 事件判定回答的是：什么时候服务质量出现了异常
- 根因定位回答的是：在这个异常窗口里，哪些用户更可能是诱因

当前事件是由 `TTFT/TPOT` 触发的，但根因分析并不直接拿用户自己的 `TTFT/TPOT` 数值做 culprit 排序，而是看下面 4 类变化：

- `RPM` 超额
- `TPM` 超额
- 输入长度变化
- 输出长度变化

### 7.2 当前采用的 4 个根因维度

对每个事件窗口、每个用户，都会计算下面 4 个窗口内的正向变化量：

- `rpm_excess_window = max(rpm - baseline_rpm, 0)`
- `tpm_excess_window = max(tpm - baseline_tpm, 0)`
- `prompt_delta_window = max(prompt_tokens - baseline_prompt, 0)`
- `completion_delta_window = max(completion_tokens - baseline_completion, 0)`

注意：

- 这里输入长度和输出长度不再乘以 `RPM`
- 只使用它们相对自身历史基线的正向变化量

这与更早版本的“长度变化乘以流量”的思路已经不同。当前实现更强调：

- 用户输入本身是否变长
- 用户输出本身是否变长

对应代码见 [latency_detector.py:484](C:/Users/10157/Desktop/xds/code/latency_detector.py#L484)。

### 7.3 从变化量到贡献占比

上面的窗口级变化量会沿时间维求和，形成每个用户在该事件窗口内的总贡献：

- `rpm_excess_sum`
- `tpm_excess_sum`
- `prompt_delta_sum`
- `completion_delta_sum`

然后分别在所有用户之间做归一化，得到 4 个贡献占比：

- `rpm_excess_ratio`
- `tpm_excess_ratio`
- `prompt_delta_ratio`
- `completion_delta_ratio`

这些占比的含义是：

- 在这次事件里，该用户占全体 `RPM` 超额的多少
- 在这次事件里，该用户占全体 `TPM` 超额的多少
- 在这次事件里，该用户占全体输入长度变化的多少
- 在这次事件里，该用户占全体输出长度变化的多少

### 7.4 按事件类型做不同权重

当前并不是一套固定权重打天下，而是根据事件更偏 `TTFT` 还是更偏 `TPOT` 使用不同权重。

对应代码见 [latency_detector.py:204](C:/Users/10157/Desktop/xds/code/latency_detector.py#L204)。

| 事件范围 | RPM | TPM | 输入长度变化 | 输出长度变化 |
| --- | ---: | ---: | ---: | ---: |
| `ttft_only` | 0.35 | 0.15 | 0.40 | 0.10 |
| `tpot_only` | 0.10 | 0.25 | 0.15 | 0.50 |
| `both` | 0.225 | 0.20 | 0.275 | 0.30 |

这里的设计思想是：

- `TTFT` 更容易受请求量和输入长度影响
- `TPOT` 更容易受 token 吞吐和输出长度影响
- 如果两类时延都异常，则折中处理

### 7.5 用户综合得分

某个用户在某个事件窗口内的根因得分为：

```text
score
= w_rpm * rpm_excess_ratio
+ w_tpm * tpm_excess_ratio
+ w_prompt * prompt_delta_ratio
+ w_completion * completion_delta_ratio
```

得分越高，说明它对本次事件的“流量变化 + 长度变化”综合贡献越大。

### 7.6 culprit 的筛选策略

不是所有有正分的用户都会展示出来，系统会继续做截断：

- 按得分从高到低排序
- 最多保留 `culprit_top_k = 3`
- 或者累计得分占比达到 `culprit_cum_ratio = 0.8` 时停止
- 除第一个 culprit 外，如果某个用户得分占比低于 `culprit_min_ratio = 0.05`，则停止继续纳入

这套规则的目的是：

- 保留最有代表性的少数用户
- 避免页面上出现长尾噪声用户

## 8. 诱因判断逻辑

当前系统会给出两种层级的“诱因判断”：

- 事件级：这次异常更像是流量变化导致，还是输入输出长度变化导致
- 用户级：某个 culprit 自身更像是流量型诱因，还是长度型诱因

### 8.1 事件级 shift score

在事件窗口内，系统会先为下面 4 类系统指标分别计算一个 `shift score`：

- `rpm_shift_score`
- `tpm_shift_score`
- `prompt_shift_score`
- `completion_shift_score`

计算方式是：

```text
positive_shift_score
= 正向增量总和 / (基线绝对值总和 + 正向增量总和)
```

也就是：

- 如果一个指标完全没有高于基线，则得分接近 0
- 如果一个指标相对基线有明显抬升，则得分会上升
- 分数是有界比例，不会无限放大

对应代码见 [latency_detector.py:262](C:/Users/10157/Desktop/xds/code/latency_detector.py#L262)。

### 8.2 事件级主导诱因

当前系统按照事件类型来解释主导诱因：

#### `ttft_only`

比较对象：

- 流量侧：`rpm_shift_score`
- 长度侧：`prompt_shift_score`

可能输出：

- `rpm_rise_dominant`
- `input_shift_dominant`
- `rpm_input_mixed`

#### `tpot_only`

比较对象：

- 流量侧：`tpm_shift_score`
- 长度侧：`completion_shift_score`

可能输出：

- `tpm_rise_dominant`
- `output_shift_dominant`
- `tpm_output_mixed`

#### `both`

比较对象：

- 流量家族：`0.5 * rpm_shift_score + 0.5 * tpm_shift_score`
- 长度家族：`0.5 * prompt_shift_score + 0.5 * completion_shift_score`

可能输出：

- `traffic_family_dominant`
- `length_family_dominant`
- `traffic_length_mixed`

### 8.3 dominant 与 mixed 的判定边界

当前 dominant 的判定规则很直接：

- 如果一侧得分至少是另一侧的 `1.2` 倍，则认为这一侧 dominant
- 否则认为是 mixed
- 如果两边都接近 0，则结果为 `unclear`

对应代码见 [latency_detector.py:277](C:/Users/10157/Desktop/xds/code/latency_detector.py#L277)。

### 8.4 用户级诱因判断

对每个 culprit，也会给一个用户级 `driver_signal`。  
它不使用系统级 shift score，而是直接使用该用户自己的 4 个贡献占比：

- `rpm_excess_ratio`
- `tpm_excess_ratio`
- `prompt_delta_ratio`
- `completion_delta_ratio`

判定方式与事件级保持一致：

- `ttft_only`：看 `RPM` vs 输入长度变化
- `tpot_only`：看 `TPM` vs 输出长度变化
- `both`：看流量家族 vs 长度家族

因此，页面上有两层结论：

- 事件卡片里的 `driver_signal` 是整个事件的系统级判断
- culprit 列表里的 `driver_signal` 是该用户自己的局部判断

### 8.5 用户级 length_signal

除 `driver_signal` 之外，当前代码里还保留了一个更偏长度视角的标签 `length_signal`，可能值包括：

- `traffic_dominant`
- `io_shift_joint`
- `input_shift_dominant`
- `output_shift_dominant`
- `length_shift_mixed`

它主要用于更细粒度地区分：

- 是输入长度在主导
- 是输出长度在主导
- 还是输入输出共同变化

对应代码见 [latency_detector.py:245](C:/Users/10157/Desktop/xds/code/latency_detector.py#L245)。

## 9. 新用户加入事件

### 9.1 背景

由于时间轴会对齐补零，因此某些用户在早期时间窗里可能全是 `0`。  
如果这类用户在后面的某个异常窗口中第一次开始产生流量，并且还被识别成 culprit，那么它和传统异常用户应该区分开。

### 9.2 识别逻辑

当前代码会先找到每个用户第一次出现正流量的位置：

```text
first_active_hour = 第一个满足 (rpm + tpm > 0) 的时间窗
```

如果某个 culprit 满足：

- `first_active_hour` 落在当前事件窗口 `[start_hour, end_hour]` 内

则认为它是本事件中的“新用户加入”。

### 9.3 输出结果

事件级会新增：

- `is_new_user_join_event`
- `event_variant`
- `new_join_users`

其中：

- 如果存在新用户，则 `event_variant = new_user_join_event`
- 否则 `event_variant = traditional_latency_event`

用户级 culprit 会新增：

- `is_new_user_join`
- `first_active_hour`

对应代码见：

- [latency_detector.py:363](C:/Users/10157/Desktop/xds/code/latency_detector.py#L363)
- [latency_detector.py:471](C:/Users/10157/Desktop/xds/code/latency_detector.py#L471)
- [latency_detector.py:553](C:/Users/10157/Desktop/xds/code/latency_detector.py#L553)

## 10. 结果产物

检测器 `detect_latency_anomalies()` 当前会返回以下关键结果：

- `time_index`
- `time_step_minutes`
- 用户矩阵：`rpm / tpm / ttft / tpot / prompt_tokens / completion_tokens`
- 系统级序列：`system_rpm / system_tpm / system_ttft / system_tpot / system_prompt_tokens / system_completion_tokens`
- 异常掩码：`sys_anom / sys_anom_ttft / sys_anom_tpot / sys_event_mask`
- `events`
- `event_reports`
- `records`
- `system_stats`
- `config_echo`
- `detection_timings`

对应返回结构见 [latency_detector.py:726](C:/Users/10157/Desktop/xds/code/latency_detector.py#L726)。

## 11. 页面展示逻辑

当前网页分析入口在 [webapp.py](C:/Users/10157/Desktop/xds/code/webapp.py#L820)。

### 11.1 分析输入

用户在网页中选择：

- 某个 Excel sheet
- 灵敏度模式
- 最大事件数

然后后端会：

1. 读取该 sheet
2. 根据灵敏度构建 `LatencyDetectorConfig`
3. 执行 `detect_latency_anomalies()`
4. 将结果写入 session
5. 跳转到结果页

### 11.2 结果页展示内容

结果页当前会展示：

- 系统指标图
- 事件卡片
- culprit 概览
- 用户汇总
- 分析耗时
- 检测拆解耗时

其中事件卡片和事件详情页会同步展示：

- 事件范围
- `rootcause_scope`
- `driver_signal`
- 流量侧比例 `traffic_driver_ratio`
- 长度侧比例 `length_driver_ratio`
- 如果是新用户加入事件，则展示 `新用户加入事件` 标识
- `new_join_users` 及首次活跃时间

相关代码见：

- [webapp.py:989](C:/Users/10157/Desktop/xds/code/webapp.py#L989)
- [webapp.py:1438](C:/Users/10157/Desktop/xds/code/webapp.py#L1438)
- [results.html:120](C:/Users/10157/Desktop/xds/templates/results.html#L120)
- [event.html:28](C:/Users/10157/Desktop/xds/templates/event.html#L28)

## 12. 耗时统计

### 12.1 分析主链路耗时

后端会统计分析请求主链路中的关键耗时，包括：

- `File Load`
- `Config Build`
- `Detection`
- `Session Build`
- `Analyze Total`

对应逻辑见 [webapp.py:840](C:/Users/10157/Desktop/xds/code/webapp.py#L840)。

### 12.2 Detection 内部分解

检测器内部还会继续拆解下面这些耗时：

- `Input Prepare`
- `Deduplicate`
- `Time Index`
- `Matrix Build`
- `System Series`
- `Event Detect`
- `Baseline`
- `Root Cause`
- `Records`
- `Stats`
- `Detection Total`

对应代码见：

- [latency_detector.py:372](C:/Users/10157/Desktop/xds/code/latency_detector.py#L372)
- [latency_detector.py:712](C:/Users/10157/Desktop/xds/code/latency_detector.py#L712)

这些耗时会在结果页上直接展示，方便定位：

- 是读文件慢
- 是检测逻辑慢
- 还是矩阵构建、去重、根因分析中的某一段慢

## 13. 当前逻辑的几个关键特点

### 13.1 事件触发和根因分析已经解耦

当前设计里：

- 事件触发依赖 `TTFT / TPOT`
- 根因分析依赖 `RPM / TPM / 输入长度变化 / 输出长度变化`

这样可以避免“用结果本身再解释结果”的循环判断。

### 13.2 输入输出长度当前只看自身变化量

当前输入输出长度的贡献不再乘以流量，而是只看：

- 相对自身历史基线的正向变化

这使得它更适合作为“用户行为模式变化”的信号，而不是“负载压力”的替代量。

### 13.3 新用户加入会被单独标注

这有助于把下面两类情况区分开：

- 老用户行为突变导致的传统异常
- 新用户上线或新业务接入导致的质量波动

## 14. 后续可能继续演进的方向

基于当前实现，后续如果需要继续增强，可以考虑：

- 给 `driver_signal` 增加更业务化的中文文案
- 将 `TTFT/TPOT` 的系统级事件分数做更细的严重等级划分
- 让 `new_user_join_event` 与传统事件支持联合展示和单独筛选
- 对 `Matrix Build`、`Deduplicate` 这些热点步骤进一步做性能优化

## 15. 对应代码位置索引

如果后续要继续开发，建议优先看这些文件：

- 检测核心：[latency_detector.py](C:/Users/10157/Desktop/xds/code/latency_detector.py)
- Web 入口：[webapp.py](C:/Users/10157/Desktop/xds/code/webapp.py)
- 结果页模板：[results.html](C:/Users/10157/Desktop/xds/templates/results.html)
- 事件详情模板：[event.html](C:/Users/10157/Desktop/xds/templates/event.html)

---

如果这份文档后续要给更多同学看，建议下一步再补一版：

- “字段样例 + 事件样例 + 根因样例”的图文说明
- 把内部标签名翻译成中文业务术语
- 增加一个“从原始 CSV 到最终事件页”的全链路流程图
