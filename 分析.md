## 检测算法整体设计

**目标**：在多租户云服务场景下，识别“系统级突发事件”以及对应的主因租户，优先保证低误报率，支撑限流 / 熔断等处置决策。

**整体思路**：

- **先看系统，再看租户**：先通过系统总量序列识别少数高置信度的“系统事件窗口”，仅在这些窗口内再做租户根因分析。
- **多信号融合**：同时利用季节性中位数、稳健 Z 分数、增长率和极值约束，多信号联合判断异常。
- **严格根因模式**：默认只在系统事件窗口内报租户异常，尽量避免对正常流量波动的误报。

---

## 模块 1：数据采集与预处理

- **输入数据结构**：多租户流量矩阵 `X[user, hour]`
  - 每行对应一个租户，列为逐小时 RPM：`H0, H1, ..., Hn`
  - 必须包含 `user_id` 列，可选 `Total` 列（7 天总量）

- **主要步骤**：
  - **字段校验**：检查是否存在 `user_id` 和足够多的 `H0..Hn` 小时列，不合法直接抛错。
  - **类型清洗**：小时列全部转为数值，非法/缺失值按 0 处理，保证数值稳定。
  - **总量补充**：如果没有 `Total` 列，则对所有小时列按行求和，得到每个租户的总请求量。

- **目的**：统一数据结构、清洗异常值，为后续所有算法提供可靠输入。

---

## 模块 2：系统流量基线与事件检测（System Event Detection）

- **输入**：矩阵 `X[user, hour]`
- **输出**：
  - 系统总量序列 `S[hour]`
  - 系统异常点 `sys_anom[hour]`
  - 系统事件窗口列表 `events = [(start_hour, end_hour)]`
  - 系统事件掩码 `sys_event_mask[hour]`

### 2.1 系统总量与增长率

- 系统总量：  
  \( S_t = \sum_u X_{u,t} \)
- 系统增长率：  
  \( g_t = \frac{S_t}{S_{t-1} + 1} - 1 \)
- 得到序列 `sys_growth`；满足 \(g_t\) 大于阈值的时刻标记为 `sys_burst`。

### 2.2 季节性基线与稳健 Z 分数

- 对每个小时 \(t\)，取过去若干天同一小时（例如每天的 10:00）的历史样本：
  - 中位数：`med[t]`，表示该时刻的季节性基线；
  - MAD（中位数绝对偏差）：`MAD(hist)`；
  - 季节性稳健 Z 分数：
    \[
    z^{\text{seasonal}}_t = \frac{S_t - \text{median(hist)}}{1.4826 \cdot \text{MAD(hist)}}
    \]
- 若季节性历史样本不足，则退化为 24 小时滚动中位数/MAD，只看过去数据，得到 `sys_rz_roll`。
- 最终系统稳健 Z 分数：
  - `sys_rz = seasonal_z（优先） 或 rolling_z（回退）`

### 2.3 ratio 主门限与系统异常点

- 计算系统总量相对季节性中位数的比值：
  \[
  ratio_t = \frac{S_t}{med_t}
  \]
- 若 `ratio_t >= sys_ratio_threshold`（例如 1.10，表示高于季节性基线 10%），则认为系统在该时刻有明显抬升，标记为 `sys_ratio_anom`。

- 系统异常点综合判定：
  \[
  sys\_anom_t =
  [ratio_t \ge \text{ratio阈值}]
  \land
  \big(
    sys\_rz_t \ge \text{Z阈值}
    \ \text{或}\
    g_t \ge \text{增长率阈值}
  \big)
  \]

### 2.4 系统事件窗口抽取与筛选

- 把连续的 `sys_anom = True` 小时合并成区间 `[start, end]`；
- 过滤掉长度小于 `event_min_len` 的短片段；
- 相邻片段间隔不超过 `event_merge_gap` 的合并；
- 对每个事件窗口，计算其内部的 `max(ratio)` 作为事件强度；
- 按强度排序，只保留前 `max_events`（如 2 个）高置信事件，得到最终 `events` 和 `sys_event_mask`。

> **业务解读**：  
> 先判断系统整体是不是“真异常事件”（超过历史同一时段 10%+，且显著偏离统计基线或增长过快），只在这些被确认的事件窗口内再做租户层排查，避免日常波动被误报。

---

## 模块 3：租户特征计算（Tenant Feature Extraction）

- **输入**：`X[user, hour]`、`S[hour]`、`sys_event_mask[hour]`
- **输出**：
  - 占比矩阵 `P[user, hour]`
  - 占比 Z 分数 `share_z[user, hour]`
  - 绝对值 Z 分数 `abs_z[user, hour]`
  - 绝对异常标记 `abs_anom[user, hour]`
  - 增长率 `growth[user, hour]` 及窗口内累计 hit `growth_burst[user, hour]`

### 3.1 租户占比（Share）

- 租户在系统中的占比：
  \[
  P_{u,t} = \frac{X_{u,t}}{S_t}
  \]
- 对每个租户的 share 序列按时间做滚动均值/标准差（只用过去数据），得到：
  - 滚动均值 `muP[u, t]`
  - 滚动标准差 `sdP[u, t]`
- share Z 分数：
  \[
  share\_z_{u,t} = \frac{P_{u,t} - \mu P_{u,t}}{sdP_{u,t}}
  \]

### 3.2 租户绝对值（Absolute）

- 对每个租户的绝对流量序列 \(X_{u,t}\) 做滚动均值/标准差（只用过去数据）：
  - `mu_abs[u, t]`、`sd_abs[u, t]`
- 绝对值 Z 分数：
  \[
  abs\_z_{u,t} = \frac{X_{u,t} - \mu_{abs,u,t}}{sd_{abs,u,t}}
  \]
- 普通绝对异常：
  - `abs_anom = X > mu_abs + user_k * sd_abs`

### 3.3 租户自身增长率（Growth）

- 租户自身增长率：
  \[
  growth_{u,t} = \frac{X_{u,t}}{X_{u,t-1} + 1} - 1
  \]
- 若增长率超过设定阈值（如 80%+），则标记 `growth_flag[u, t] = True`；
- 在一个增长窗口（如 3 小时）内，对 `growth_flag` 做滚动求和，得到 `growth_hits` 与 `growth_burst`，表示这一小段时间内是否持续在“往上冲”。

> **业务解读**：  
> 对每个租户，我们既看它的“绝对流量是不是异常高”，也看它在整个系统中“占比是不是异常高”，再结合它自己的“增长速度”作为三条证据链。

---

## 模块 4：租户异常打分（Tenant Anomaly Scoring）

- **输入**：租户特征（`share_z`、`abs_z`、`growth` 等）、`sys_event_mask`
- **输出**：租户-时间二维异常矩阵 `flags[user, hour]`

### 4.1 严格根因模式（strict_rootcause）

- 构造 `sys_anom_mask = sys_event_mask[np.newaxis, :]`，将系统事件掩码扩展到租户维度；
- 在严格根因模式下（`strict_rootcause=True`），只在 `sys_event_mask = True` 的小时内考虑租户异常；
- 事件外所有 `flags[user, hour]` 统一视为正常，即使单租户有轻度波动也不报。

### 4.2 事件内复合判定规则

- 在系统事件窗口内，对每个租户使用如下规则：
  \[
  flags\_overload =
  sys\_event\_mask
  \land
  \Big(
     (share\_z > \text{阈值} \land (growth\_burst \ \text{或} \ share\_z > \text{极值阈}))
     \ \text{或}\
     abs\_z > \text{极值阈}
  \Big)
  \]

- 口语解释：
  - **门槛 1**：必须发生在“系统已经确认异常”的时间段里；
  - **门槛 2**：满足下面任一条即认为该租户“在本次系统事件中高度可疑”：
    - 它在系统中的占比显著抬升，且在这段时间内自身流量持续快速增长；或占比异常极端；
    - 它在该小时的绝对流量远超自己历史水平（绝对 Z 极端大）。

> **业务解读**：  
> 我们不在平时乱报租户，只在“系统整体已经出事”的那几个时间段里，筛选出在占比和增长上最异常的 1～2 个租户，作为根因候选。

---

## 模块 5：异常 Episode 峰值回填（Episode Peak Backfill）

- **问题背景**：最陡的“斜率点”和最高的“峰值点”不一定是同一小时，只看增长率可能只标记开头而漏掉峰值。
- **算法步骤**：
  - 对每个租户，基于 `abs_anom` 或 `abs_z > 某较低阈值` 构造若干连续的“异常 episode” 区间；
  - 如果某个 episode 中**已经存在至少一个小时被上一模块判定为异常**：
    - 在该 episode 内找到绝对流量 `X[u, t]` 的最高点，将对应的小时也强制标为异常。
- **效果**：在可视化中，一段异常波形的最高点一定会被高亮出来，便于业务侧关注“最严重的那一刻”。

---

## 模块 6：系统事件根因定位（Root Cause Localization）

- **输入**：`events`、矩阵 `X`、基线 `mu_abs`、异常标记 `flags`
- **输出**：`event_reports`（每个系统事件的主因租户列表与细节）

### 6.1 超额流量视角

- 对每个系统事件窗口 `[a, b]`：
  - 计算租户在该窗口内相对于自身均值的超额流量：
    \[
    excess_{u,t} = \max(X_{u,t} - \mu_{abs,u,t}, 0)
    \]
  - 对时间求和，得到该事件期内租户的总超额：
    \[
    excess\_sum\_user[u] = \sum_{t=a}^{b} excess_{u,t}
    \]
  - 总超额：
    \[
    total\_excess = \sum_u excess\_sum\_user[u]
    \]

### 6.2 贡献度排序与主因筛选

- 按 `excess_sum_user[u]` 从大到小排序：
  - 选取前 `culprit_top_k` 个左右的租户；
  - 要求累计贡献比例达到 `culprit_cum_ratio`（如 70% 左右）；
  - 且后续候选的单个贡献比例 ≥ `culprit_min_ratio`（如 5%），避免把“边缘小角色”标成主因。
- 对每个入选租户，记录：
  - `excess_ratio`：该租户对本次事件“额外流量”的贡献占比；
  - `peak_hour`、`peak_rpm`：该租户在本事件窗口中的自身峰值时间和流量。

> **业务解读**：  
> 把系统这一段“多出来的请求量”分摊到各个租户身上，看是谁贡献得最多，从而形成“主犯/次要责任方”的列表，并给出它在事件期内的峰值表现。

---

## 模块 7：结果汇总与处置建议（Reporting & Mitigation Suggestions）

- **系统级指标**：
  - 整体小时数、平均 RPM、P95、最大值；
  - 检出的系统事件个数和持续小时数；
  - 被判定为异常租户的数量及占总租户数比例。

- **租户级指标**：
  - 每个租户：
    - 命中异常的小时数与具体小时列表；
    - 事件内 / 事件外的命中分布；
    - share / 绝对值 / 增长三条证据的最大强度；
    - 平均 / 峰值 / P95 RPM；
    - 文本化“异常原因标签”（如“系统过载期间占比异常+快速放量”）。

---

### 7.1 异常作用域（scope）标注

- **scope 字段**：为每条异常记录（系统 / 租户）打上作用域标签，用于后续处置分层：
  - `scope = system`：系统整体事件（如 `system_overload_hours` 非空）；
  - `scope = tenant`：单租户在事件内/外的异常（当前模拟数据主要体现这一层）；
  - `scope = traffic`：流量工程 / 路由不均衡导致的异常（在真实多 Region / 多集群场景中使用，本模拟数据未建模此维度）。
- **基本原则**：  
  - 先判断是否存在系统级事件（system scope）；  
  - 在系统事件窗口内命中的租户异常，按“系统事件中的主因租户”归入 `tenant` scope，同时在系统事件报告中引用；  
  - 未来在引入 Region / 集群等维度后，对明显的“流量倾斜 / 路由异常”单独打上 `traffic` scope。

---

### 7.2 系统层处置建议（scope = system）

- **处置目标**：优先保证平台整体 SLO，不让系统本身被拖垮。
- **适用对象**：系统总量 `S(t)` 在较长时间内显著超出基线、形成 `events` 的全局事件。
- **建议动作**：
  - **快速止血**：
    - 启动平台级降级策略（关闭/限速非核心功能模块，如报表、批量导出等）；
    - 检查核心依赖（存储、缓存、队列）的容量和错误率，必要时优先扩容热点集群；
    - 启用全局级别的保护开关（例如整体并发上限、全局限流兜底配置）。
  - **容量与配置调整**：
    - 针对事件窗口内 `max(ratio)` 较高的时段，评估是否需要提高容量基线（长期扩容）；
    - 检查系统级超时、重试、连接池等参数是否过于激进，避免“放大抖动”；
    - 对关键路径增加冗余或自动故障转移（如多集群、多 Region 容灾）。
  - **系统视角复盘**：
    - 对每次系统事件沉淀“系统级应急预案模板”，包括谁负责、多久内响应、兜底开关位置；
    - 结合事件强度和频率，更新容量规划和 SLA 预期。

---

### 7.3 租户层处置建议（scope = tenant）

- **处置目标**：在**保护平台和其他租户**的前提下，帮助问题租户控制自身行为并恢复。
- **适用对象**：在系统事件窗口内被识别为主因租户，或在事件外反复出现强烈绝对异常/增长异常的单租户。
- **建议动作**：
  - **平台侧保护**：
    - 按 `excess_ratio`（超额流量贡献度）分强/中/轻三档，对该租户应用不同强度的限流 / 熔断 / 冷却时间策略；
    - 对高危租户（多次作为主因出现）可以下调调度优先级，在高峰期限制其批量任务或重流量操作；
    - 为该租户配置单独的熔断/降级策略（如读降级为缓存、异步化处理非关键请求）。
  - **租户侧自查建议**：
    - 给出清晰的“异常原因标签”和证据（如“系统过载期间占比异常 + 自身流量 3 小时内连续增长 80%+”）；
    - 建议租户排查：突发任务、代码发布、批量导入导出、自动重试策略等可能放大流量的行为；
    - 提供接入侧建议：加入客户端限流、指数退避重试、按优先级拆分不同调用通道等。
  - **策略沉淀**：
    - 对多次成为主因的租户，可与业务方约定“高峰期使用规范”和“突发容量申请流程”；
    - 在告警平台中为租户异常配置模板化处置手册（谁响应、先做哪些操作、怎么沟通）。

---

### 7.4 流量工程处置建议（scope = traffic）

> 说明：当前模拟数据未显式建模多 Region/多集群维度，以下为真实线上环境中的扩展设计，用于指导流量工程策略。

- **处置目标**：通过**调度和整形流量**，避免某些 Region / 集群 / 路径过载，提升整体资源利用率和稳定性。
- **典型触发场景**（未来在检测结果中标记为 `scope = traffic`）：
  - 某 Region / 集群的流量占比在短时间内异常升高，负载显著不均衡；
  - 某条路由（如某个 API 网关入口、某类请求）出现明显流量畸高，而其他路径闲置；
  - 蓝绿 / 灰度发布阶段，流量没有按预期在新旧版本之间分配。
- **建议动作**：
  - **调流向**：
    - 调整跨 Region / 集群的权重，将部分流量迁移至负载较低的资源池；
    - 对异常路由进行限速或改路由，避免单一入口被打穿；
    - 在流量切换（灰度/回滚）时采用渐进式导流和健康检查，避免一次性流量骤变。
  - **流量整形与削峰填谷**：
    - 对可延迟的批处理/训练任务进行错峰调度，将其移动到低谷时段；
    - 应用令牌桶/漏桶等算法对突发流量做平滑，限制瞬时爆发；
    - 对高峰期请求设置更严格的并发上限和排队策略。
  - **流量工程复盘**：
    - 基于每次 `traffic` scope 事件，优化路由策略、分区策略与发布策略；
    - 将关键 API/租户的“流量画像”纳入容量规划，提前评估可能的热点和倾斜。

---

> 总结：  
> 检测模块本身只负责标记“哪里不正常”（系统 / 租户 / 流量形态），而处置模块则根据 `scope` 和异常强度，分别给出系统层、租户层和流量工程三类动作建议，形成可执行的 SRE/运维手册模板。

